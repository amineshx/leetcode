/**
 * @param {number} n
 * @param {number} time
 * @return {number}
 */
var passThePillow = function(n, time) {
    rounds = Math.floor(time/(n-1)+1)
    k=time%(n-1)
    if(rounds%2===0){
        return n-k
    }else{
        return 1+k
    }
};
