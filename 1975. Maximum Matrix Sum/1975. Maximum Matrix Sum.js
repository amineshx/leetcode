/**
 * @param {number[][]} matrix
 * @return {number}
 */
var maxMatrixSum = function(matrix) {
    let totalSum = 0
    let min_abs = Infinity
    let negative_nums = 0
    for (row of matrix){
        console.log(row)
    }
};

maxMatrixSum(matrix = [[1,2,3],[-1,-2,-3],[1,2,3]])